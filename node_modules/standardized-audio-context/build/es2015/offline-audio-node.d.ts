export declare class OfflineAudioNodeProxy {
    private _channelCountMode;
    private _channelInterpretation;
    private _fakeNodeStore;
    private _numberOfInputs;
    private _numberOfOutputs;
    constructor({channelCountMode, channelInterpretation, fakeNodeStore, numberOfInputs, numberOfOutputs}: {
        channelCountMode?;
        channelInterpretation?;
        fakeNodeStore;
        numberOfInputs?;
        numberOfOutputs?;
    });
    channelCountMode: any;
    channelInterpretation: any;
    numberOfInputs: any;
    numberOfOutputs: any;
    connect(destination: any, output?: number, input?: number): any;
    disconnect(destination: any): any;
}
