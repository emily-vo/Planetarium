import { OpaqueToken } from '@angular/core';
export declare const unpatchedAudioContextConstructor: OpaqueToken;
export declare const UNPATCHED_AUDIO_CONTEXT_CONSTRUCTOR_PROVIDER: {
    deps: OpaqueToken[];
    provide: OpaqueToken;
    useFactory: (window: any) => any;
};
