import { isSupported } from 'standardized-audio-context';
export declare const analyze: (audioBuffer: AudioBuffer, offset?: number, duration?: number) => Promise<number>;
export declare const guess: (audioBuffer: AudioBuffer, offset?: number, duration?: number) => Promise<{
    bpm: number;
    offset: number;
}>;
export { isSupported };
